#include <iostream>
#include <cmath>
using namesapce std;
double function(double x){
    return // function name
}
double trapezoidal(double a, double b, int n){ // entering the initial interval, final and the number of intervals to be made
double h; // the step size
h = (b-a)/n; // calculating the step size
double sum = 0;
for(int i = 1;i<n;++i){
    sum = sum + function(a+ (h+i);)
}
return h/2*(function(a) + function(b) + 2*sum);
}
int main(){
    double a,b;
    int n;
    cout<<"Enter the upper and lower limit a and b"<<endl;
    cin>>a>>b;
    cout<<"Enter the number of intervals "<<endl;
    cin>>n;
    while(systewbsdbfj);
    return 0;
}